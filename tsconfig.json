{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "noUncheckedIndexedAccess": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    /* Linting */
    "strict": true
    // "noUnusedLocals": true,
    // "noUnusedParameters": true,
    // "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*", "src/.eslintrc.ts", "pfr/unkown/NF-uk.tsx", "pfr/unkown/SelectNF-uk.tsx"],
  "exclude": ["node_modules", "Remainders/**", "node:sqlite"]
}

// {
//   "compilerOptions": {
//     /* Target & Compatibility */
//     "target": "ES2022", // Use ES2022 for better performance and features like top-level await.
//     "useDefineForClassFields": true, // Aligns with modern JS class fields behavior.
//     "lib": ["ES2022", "DOM", "DOM.Iterable"], // Includes modern libraries.

//     /* Module System */
//     "module": "ESNext", // ESNext is great for bundlers like Vite, Webpack, or Rollup.
//     "moduleResolution": "bundler", // Optimized for modern bundlers.
//     "allowImportingTsExtensions": true, // Useful for ESM setups with TypeScript.
//     "isolatedModules": true, // Required for compatibility with Babel or SWC.
//     "moduleDetection": "force", // Ensures strict adherence to module imports.
//     "baseUrl": ".", // Simplifies imports with base path.
//     "paths": {
//       "@/*": ["src/*"] // Helps in creating cleaner, alias-based imports.
//     },

//     /* Emit Behavior */
//     "noEmit": true, // Prevents emitting compiled JavaScript files during development.

//     /* JSX Settings */
//     "jsx": "react-jsx", // Use the modern JSX runtime for React (React 17+).

//     /* Strict Type Checking */
//     "strict": true, // Enables all strict type-checking options.
//     "noUncheckedIndexedAccess": true, // Catches unsafe indexed property access.
//     "skipLibCheck": true, // Speeds up compilation by skipping library type checking.

//     /* Linting & Quality */
//     "noUnusedLocals": true, // Warns about unused variables.
//     "noUnusedParameters": true, // Warns about unused function parameters.
//     "noFallthroughCasesInSwitch": true, // Ensures safe switch case statements.

//     /* Performance */
//     "incremental": true, // Speeds up subsequent compilations.
//     "tsBuildInfoFile": "./.tsbuildinfo", // Stores build info for incremental builds.
//     "forceConsistentCasingInFileNames": true // Prevents case-sensitivity issues.
//   },
//   "include": ["src/**/*"],
//   "exclude": ["node_modules", "Remainders/**", "node:sqlite"]

// }
